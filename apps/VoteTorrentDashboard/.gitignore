# The root .gitignore's bare `dist` rule matches only an entry named exactly
# `dist` — it does not match `dist-mutant-*` (53-11, D-20). Kept app-local
# (never added to the shared repo-root .gitignore) because that file is
# shared with a concurrent session in this checkout, matching 53-07's own
# precedent for apps/VoteTorrentPublic/.gitignore's `dist-gate` rule.
dist-mutant-*
