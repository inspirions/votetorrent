# macOS Finder metadata
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# TypeScript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test

# parcel-bundler cache (https://parceljs.org/)
.cache

# next.js build output
.next

# nuxt.js build output
.nuxt

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TypeScript output
dist
out

# Vendored portal packages ship prebuilt dist — keep them tracked (overrides the `dist` rule above).
!vendor/**/dist
!vendor/**/dist/**

.planning

.claude

# Android bugreport dumps (timestamped, never commit)
bugreport-*.zip
**/bugreport-*.zip

#figma token
figmaToken.txt

# Temp Figma REST artifacts (local only)
.figma-tmp/

.yarn/*
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
!.yarn/patches

.idea

# proof-flags.generated.ts is TRACKED (committed default-false baseline) — an
# ignore entry here would be a no-op for a tracked file (WR-02, 17-REVIEW).
# Local overrides written by run-vtest02.sh / run-dial-probe.sh are restored
# to the default-false content by each script's EXIT trap.

vendor
scratchpad/