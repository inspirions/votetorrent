{
  "_readme": [
    "These are MEASURED baselines, not targets. Each number here was captured by actually",
    "running the named command and reading its output on the date recorded in the entry's",
    "measuredOn field.",
    "Changing any number in this file is a deliberate act that belongs in the SAME commit as",
    "whatever changed the underlying measurement (a fix, a new test, a dependency bump). This",
    "file is never edited to make a red CI run go green without that accompanying change.",
    "A DROP in a failure count is as much a reason to investigate as a RISE. A failure that",
    "disappeared without a commit that fixed it is a signal, not a free pass — see voteEngine's",
    "exact (not floor) check on both count and titles.",
    "scripts/assert-ci-baselines.mjs reads this file by import.meta.url, never mutates it, and",
    "has no 'update the baseline' mode. Updating a baseline is a human, reviewed commit."
  ],
  "voteEngine": {
    "command": "yarn workspace @votetorrent/vote-engine test",
    "measuredOn": "2026-09-01",
    "minPassing": 1656,
    "ciMinPassing": 1655,
    "expectedFailing": 9,
    "knownFailureTitles": [
      "AuthorityEngine schema constraints - InviteSlot table should reject update or delete of InviteSlot (InsertOnly)",
      "AuthorityEngine admin signing flow should reject update or delete of AdminSigning (InsertOnly)",
      "D-01 compat gate: cid()/cid_decode registration + Wave 0 open-question probes Probe 3b: the bare RESEARCH Pattern 3 form json_extract(cid_decode(x),\"$.codec\")='raw' under-accepts even VALID input — Plan 02 must use cast(...as text)",
      "post-peerId-patch blocker 2: cadre-core hardcodes clusterSize and drops allowUnvalidatedSmallCluster both createLibp2pNode call sites still hardcode clusterSize: 3",
      "Optimystic plugin — composite-PK DELETE derives the wrong storage key (no-op delete) BUG: DELETE key (extractPrimaryKey on compacted PK-only oldKeyValues) does NOT match the stored key",
      "Optimystic plugin — composite-PK DELETE derives the wrong storage key (no-op delete) CONTROL: single-column PK at index 0 is unaffected (explains why only composite PKs break)",
      "P2P-11 residual: a relay-only peer has no address SEED (db-p2p 0.24.0) BLOCKER: findCluster sources a record's multiaddrs solely from what THIS node already knows",
      "P2P-11 residual: a relay-only peer has no address SEED (db-p2p 0.24.0) BLOCKER: no seeding path exists from a circuit-relay reservation into the peer address book",
      "P2P-11 residual: a relay-only peer has no address SEED (db-p2p 0.24.0) documents the exact installed version this residual was measured against"
    ],
    "note": "Re-measured 2026-09-01 for the @optimystic 0.24.2 -> 0.25.1 / p2p-fret beta.3 -> beta.4 bump. Live run: 1656 passing / 72 pending / 9 failing.\n\nexpectedFailing 8 -> 9. The added title is the THIRD P2P-11 residual tripwire ('no seeding path exists from a circuit-relay reservation into the peer address book'), which flipped on the bump. It is a source-SHAPE tripwire over installed db-p2p, not a behavioural test, and it flipped because 0.25.1 reshaped Libp2pKeyPeerNetwork.findCluster -- classifySelfDialability now splits the old `parsed.length === 0` count into `addressless` vs `selfRelayOnly`. The residual itself is NOT fixed: 0.25.1 still assigns the locally-parsed addresses straight into the record (libp2p-key-network.js:889) and still merely COUNTS addressless members (:886, :895) rather than seeding them. It is carried as a known failure rather than re-anchored because the spec's own sibling test demands a device n=4 re-proof before the assertions are rewritten against a new version -- that proof has not been run. Re-anchor all three P2P-11 tripwires together, after it.\n\nSIX failures were RETIRED in this same commit, which is why 9 is not 15. Three were version pins reconciled to the new line (no-portal-vendor-regression PUB-01-c, and published-stack-lock-regression PUB-02 x2). Three were the gossipsub/libp2p-3 skew tripwires (post-peerId-patch blocker 3), whose subject db-p2p 0.25.1 removed outright -- the block was rewritten from 'the skew still exists' to 'gossipsub is absent and must not return', so the coverage is inverted, not deleted. The dead @chainsafe/libp2p-gossipsub yarn-patch resolution was dropped from root package.json in the same commit; the .patch file is retained on disk.\n\nminPassing 1411 -> 1656 (ciMinPassing 1410 -> 1655). The 1411 figure was measured 2026-08-26 and had gone stale as later waves added tests, leaving the floor ~245 tests loose -- it would not have caught a mass disappearance. Raised to the live measurement here because this is already a reviewed baseline commit. The deliberate ciMinPassing = minPassing - 1 relationship is UNCHANGED and still has the same cause, restated: `.planning/` is a nested git repo the outer repo gitignores, so a fresh CI checkout never has it, and strand-cohort-routing-coverage.spec.ts's 'the diagnosis doc is present...' test calls this.skip() when absent -- reporting as pending on CI instead of passing. expectedFailing stays exactly 9 in BOTH environments; only the passing/pending split moves."
  },
  "dashboardTier1": {
    "command": "yarn workspace votetorrent-dashboard test:node",
    "minPassing": 435,
    "maxFailing": 0,
    "maxSeconds": 45,
    "receiptMarker": "tier1-logic",
    "note": "Re-measured 2026-09-02 at 435 passing / 0 failing (`yarn workspace votetorrent-dashboard test:node`), raising minPassing from 426. This is 53-08 (D-19/D-23/D-24), the shared browser-gate runner: test/node/ui-gate-harness.test.mjs adds 9 new cases (the stylesheet-import assertion, the tokens.css-absence assertion plus its positive control, the vite.gate.config.ts dedupe-absence assertion plus its positive control, the outDir/input assertion, the gate:ui script-presence assertion, the canonical-token-form assertion, and the components.js export-echo assertion). packages/ui-web's own workspace test run (`yarn workspace @votetorrent/ui-web test`) rose 99 -> 129 in the same plan (serve-dist.test.mjs +5, tokens-parse.test.mjs +14, gate-source-integrity.test.mjs +11) -- that is a SEPARATE workspace run and is not counted in this dashboardTier1 number; see uiWebTier1's own note. The by-hand D-23 inversion proof (Task 3) was run live: with the dashboard's own `@import '@votetorrent/ui-web/tokens.css';` commented out of src/app.css and `gate:ui` re-run, token-declared-values and base-rule-used-values both FAILED (exit 1); the line was restored (`git diff --exit-code -- apps/VoteTorrentDashboard/src/app.css` succeeded) and `gate:ui` re-run green (exit 0, tokens=31). The canonical token-acquisition form (`@import` as the first statement of app.css) was already in place from 53-03 -- no change needed. Prior 426 note follows.\n\nRe-measured 2026-09-02 at 426 passing / 0 failing / ~22.2s wall (`yarn workspace votetorrent-dashboard test:node`), lowering minPassing from 460. This is 53-05 (D-01/D-02/D-07/D-25), the component-and-lifecycle RELOCATION into packages/ui-web -- src/lifecycle/election-phase.js, src/screens/AdvisoryDisclosure.tsx and src/screens/panels/LifecyclePill.tsx were deleted with no shim; election-phase.js now lives at packages/ui-web/src/lifecycle/election-phase.js behind a new ./lifecycle export, and the two components live at packages/ui-web/src/components/{AdvisoryDisclosure,LifecyclePill}.tsx behind ./components. The delta is exactly -34, term by term: -32 for the 32 election-phase.js unit cases that moved verbatim into the new packages/ui-web/test/election-phase.test.mjs (the two seed-fixture cases that stayed in the dashboard did not leave the workspace -- they were renamed, not deleted, into the new test/node/seed-election-surface.test.mjs, so they are not part of this delta); -2 for the two AdvisoryDisclosure.tsx source cases in test/node/preview-control.test.mjs ('AdvisoryDisclosure.tsx renders gate.advisoryDisclosure exactly once with no branch around it' and its positive control), re-homed and strengthened into packages/ui-web/test/shared-components.test.mjs; -1 for the 'LifecyclePill.tsx returns null for a null phase' case in test/node/election-ops-panels.test.mjs, likewise re-homed; +1 for a new inertness control in preview-control.test.mjs proving the tightened 'DashboardShell.tsx renders ... an explicit-voice AdvisoryDisclosure' matcher does NOT accept a bare zero-prop mount. Sum: -32 -2 -1 +1 = -34; 460 - 34 = 426, matching the live measurement exactly. The corresponding rise lives in the new uiWebTier1 note below (+32 election-phase, +16 shared-components.test.mjs, +2 copy.test.mjs single-delta assertions narrowing 53-04's never-actually-landed key-set-equality check into a named single-delta expectation). Two directory-scoped dashboard guarantees (gate-contract.test.mjs's C1 module-format contract and preview-control.test.mjs's elevation-of-privilege confinement walk) were extended to cover packages/ui-web/src/lifecycle, proven live by a planted-and-reverted probe.ts in each case -- neither edit added or removed a test case. Prior 460 note follows.\n\nRe-measured 2026-09-02 at 460 passing / 0 failing / ~22.2s wall (`yarn workspace votetorrent-dashboard test:node`), lowering minPassing from 476. This is 53-04 (D-11/D-25), the copy-table RELOCATION into packages/ui-web -- apps/VoteTorrentDashboard/src/i18n/copy.js was deleted with no shim and its 73-key COPY table now lives at packages/ui-web/src/copy.js, re-exported as @votetorrent/ui-web's `.` entry point. This is a relocation, not a loss: 16 tier-1 cases whose SUBJECT is the table or t() itself moved verbatim into packages/ui-web/test/copy.test.mjs (see the new uiWebTier1 entry below) -- by name: 't(\\\"bootstrap.cta\\\") returns exactly \\\"Redeem Code\\\"', 't(\\\"gate.badgeReal\\\") and t(\\\"gate.badgeSimulated\\\") return the D-18 binding wording', 't(\\\"network.forgetConfirmBody\\\", {...}) interpolates and leaves no residual {{', 't(\\\"does.not.exist\\\") throws an Error naming the missing key', 't(\\\"network.forgetConfirmBody\\\") with no params throws naming the unresolved placeholder', 'every value in COPY is a non-empty string', 'no COPY value contains a GSD decision ID (D-\\\\d{2})', 'no COPY value contains a GSD phase number (\\\"Phase \\\\d+\\\")', 'no COPY value matches /read-only/i (D-17: no read-only panel state exists)', 'COPY is frozen -- assigning a new key throws in strict mode', 'panelFrame.tierPill, panelFrame.sitePill and panelFrame.sitesPill exist and interpolate their placeholders', 't(\\\"network.swapErrorHeading\\\") and t(\\\"network.swapErrorBody\\\") resolve to non-empty strings with no residual {{', and the whole D-25 refusal-family block's four tests ('D-25: every refusal heading and body resolves to a non-empty string carrying no residual {{', 'D-25: the three refusal HEADINGS are pairwise different', 'D-25: the three refusal BODIES are pairwise different', 'D-25: each refusal body carries its OWN action token and neither of the other two'). The sum-invariant holds exactly: 460 (this floor) + 16 (named above, now inside uiWebTier1's own 42-case floor) = 476, the pre-move dashboard figure, live-measured on both sides of the move in the same session. The dashboard KEEPS every assertion whose subject is the dashboard's OWN use of the table: the BOOTSTRAP_PHASES mechanical-coverage test and its unmapped-phase throw sibling, the whole copyKeysForOutcome family (three-status selection, missing-status throw, ok-status throw, unmapped-status-naming throw, every-other-outcome-unchanged, the outcome/reason/status totality test), the bootstrap.errorInvalidCodeCta-survives and errorInvalidCodeBody-local-case tests, the T-52-12-01 machine-identifier gate, the 'already been used' whole-src scan (now reading uiWebSrc('copy.js') through the 53-01 resolver instead of a local src/i18n path), and the three Bootstrap.tsx source-wiring tests plus their inertness control -- all rewritten to import { COPY, t } from '@votetorrent/ui-web' and to resolve every path through dashboardSrc()/uiWebSrc(). Prior 476 note follows.\n\nRe-measured 2026-09-02 at 476 passing / 0 failing / ~19.8s wall (`yarn workspace votetorrent-dashboard test:node`), raising minPassing from 469. This is 53-03 (D-15, the tokens.css/app.css split), and the delta is exactly the 7 new cases in 53-03 Task 2's test/node/app-css-split.test.mjs -- a positive control and its benign var(--...) inertness control for the declaration matcher, the zero-:root and zero-declaration checks (references via var(--...) still pass), the canonical @import position assertion plus its would-fail-if-absent control, and the .layout/900px-collapse/.panel-grid survival check. packages/ui-web/test/tokens.test.mjs (also added by 53-03 Task 2) is a SEPARATE workspace test run (`yarn workspace @votetorrent/ui-web test`) and is not counted in this dashboardTier1 number. Confirmed live that no gate in 53-03 proves the tokens resolve to their declared values in a browser -- that remains 53-08's D-23 probe. Re-measured 2026-09-01 at 469 passing / 0 failing across three consecutive local runs (~20.7s, ~20.7s, ~20.5s wall), raising minPassing from 462. This is 53-01, discharging D-03 (the shared repo-root source-path resolver, scripts/lib/source-paths.mjs) and the stay-and-repoint half of D-25. The delta is exactly the new test/node/source-paths.test.mjs's 7 cases and NOTHING else: the 18 other repointed files (authority-admin-panels, authority-admin-queries, bootstrap-redemption, capabilities, copy, db, db-delete, election-ops-panels, gate, gate-contract, no-qr-affordance, preview-control, preview-scopes, reads, refresh-swap, registry, shell-wiring, snapshot-restore) were edited in their path-construction layer only -- every assertion, test name and per-file test count is unchanged from the prior 462 floor. The resolver landed BEFORE any file move by design (53-CONTEXT.md's Risks section: land the resolver before the move, not after), so a later drop below 469 during Phase 53 waves 2-4 means a move broke a path, not that a test was removed. Prior 462 note follows.\n\nRe-measured 2026-09-01 at 462 passing / 0 failing / ~21s wall, raising minPassing from 444. The floor is raised here because the checker's own notice asked for it deliberately, and because this commit is the one that returns the gate to zero failures: capabilities.js had been stale against votetorrent.qsql since 2026-08-27, when 97acf6f0 (AssociationRequest) and 0da5b2bd (ElectionRecordValidityPolicy) added scope-enforcement sites and 745a1e57 regenerated schema-sql.ts without regenerating the capability matrix. Total enforcement sites 41 -> 45 (vrg 16 -> 18, mel 10 -> 12). Note that regenerating ALONE takes this gate from 1 failure to 5: the generated file is a hub, and src/reads/registrations.js's hand-maintained SURFACE_COUNT_QUERIES map had silently lost lockstep with the DERIVED TABLES_READ, so the new vrg table reached db.prepare(undefined). Original note follows.\n\nNyquist gap-closure round 4 (2026-08-26), FINAL STATE 444 pass / 0 fail: the escalated implementation fix landed in this same commit -- DashboardShell.tsx:623 now logs `console.error(err.name)` only, matching its three siblings at :394/:584/:608, so the deliberately-red widened matcher is green and minPassing/maxFailing were returned to 444/0 exactly as the escalation required. Re-measured live after the fix: `node --test --test-concurrency=1 test/node/*.test.mjs` -> 444 pass / 0 fail / ~20.6s wall. The intermediate 443/1 state described below is retained for the record because it documents WHY the matcher was widened and what it caught. Original round-4 note follows: raised 439 -> 444 total (443 pass / 1 fail), re-measured live via `node --test --test-concurrency=1 test/node/*.test.mjs` three times (~19.5-20.1s wall each). GAP 1 (D-03) added test/node/no-qr-affordance.test.mjs, 5 new cases: a positive control against a planted QR/camera sentinel, an inertness control, the self-tripping-trap proof (DashboardSignInCodeScreen.tsx's own D-03 comment matches the RAW source but not the comment-stripped source), and the real absence scan over both sign-in surfaces (Bootstrap.tsx + apps/VoteTorrentAuthority/src/screens/dashboard/*.tsx) -- all 5 pass. GAP 4 (RF-03) widened shell-wiring.test.mjs's RAW_MESSAGE_RE from the narrow `err instanceof Error ? err.message` ternary shape to any bare `err(or)?.message` token inside a console.error(...) call -- this correctly and deliberately turns 'no panel and not the shell logs a raw database error MESSAGE to the console' RED, naming DashboardShell.tsx (the real site is :623, `console.error(err.name, err.message)`, which the old matcher's ternary-only shape could not see). maxFailing raised 0 -> 1 and minPassing 439 -> 443 (the passing count, not the total) to reflect this ONE deliberately-red, escalated test -- it is not a regression and must not be silenced by weakening the matcher; it should return to maxFailing: 0 / minPassing: 444 in the SAME commit that makes DashboardShell.tsx:623 log `err.name` only, matching its three siblings. See VALIDATION.md's GAP 4 entry for the escalation. GAP 2 (D-18 browser leg, WR-04/RF-04) and GAP 3 (RF-01) added no test:node cases -- their evidence lives in test/browser/compose-gate.tsx (PHASE 10's new rungs 9-12) and apps/VoteTorrentDashboard/scripts/assert-no-test-harness-in-dist.mjs respectively, so this count is unaffected by either. Prior history retained verbatim below. 50-04's original local budget was 5s at 12 tests. The suite has grown to 326 tests across 50-05/50-08/50-09/50-12, and a live re-run during 50-13 measured ~17.5s internal / ~18s wall on this machine three times in a row. maxSeconds was therefore set to 45 (not the phase-history figure of 15, which is now stale and would fail on a clean run before any CI slowness is even added) to give real CI headroom over the current measured cost. The bound exists to keep tier 1 a fast gate -- exceeding it is a signal the tier is drifting into something else, not a number to raise reflexively. See the 50-13 SUMMARY's Deviations section for the full measurement. Raised 326 -> 330 by the code-review fix for CR-01, which added four assertions to test/node/preview-control.test.mjs (the re-seed matcher, its inertness control, the dead-prop matcher and its inertness control) -- re-measured live at 330 pass / 0 fail / ~19.5s in the same commit that added them. Raised 330 -> 332 by the fix for WR-02, which added a copyKeyForPhase totality test and its unmapped-phase control, then 332 -> 334 by WR-04 (the relabelled-envelope refusal and its clean-bootstrap positive control) 334 -> 335 by WR-05 (the blocked-delete record-survival test) 335 -> 337 by WR-06 (the non-creating listObjectStores probe and its present-database positive control) and 337 -> 341 by WR-10 (the new test/node/shell-wiring.test.mjs source suite, two matchers plus two inertness controls), then 341 -> 344 by WR-11 (the handle-handover matcher, its close-after inertness control and the both-call-sites check), then 344 -> 347 by WR-12 (panel console hygiene, its inertness control and the nine-site count) and 347 -> 350 by WR-16 (the banner-default matcher, its old-shape inertness control and the distinct-copy check) 350 -> 351 by WR-17 (the applied-count test) and 351 -> 355 by WR-18 (the empty-expected-name refusal, its non-empty positive control, and the shell-side guard plus its inertness control). Raised 355 -> 364 by 50-14 Task 1's CR-01 gap closure: 9 new `resyncRealScopes` cases in test/node/preview-scopes.test.mjs (referential stability, the CR-01 empty-to-nine transition, the sticky touched guard plus its positive control, order-insensitivity, duplicates, validation parity, and the frozen/no-mutation check) -- re-measured live at 364 pass / 0 fail / ~18-19s wall in the same commit range that added them. preview-control.test.mjs's existing assertions were updated in place (not added), so its count is unchanged. Raised 364 -> 370 by 50-16's gap-closure plan: Task 1 added 5 new refresh-swap.test.mjs cases proving the CR-02 repair path (an explicit-empty-counts variant of the row-count record, a genuine post-swap divergence, the unheld-network throw, a storage-sequence-log proof of read-after-write with zero extra writes, and a direct write-count assertion on a verify-failed refusal), and Task 2's copy.test.mjs rewrite net +1 (two copyKeyForPhase tests replaced by three: the mechanical bootstrap.phase.<value> coverage test, its unmapped-key throw, and a new panelFrame.*Pill interpolation test) -- re-measured live at 370 pass / 0 fail / ~19.8s wall in the same commit range that added them. Raised 370 -> 372 by 50-17 Task 1's WR-06 gap-closure: two new db-delete.test.mjs cases (the immediately-after-deleteNetworkDb listObjectStores sequence, and the indexedDB.databases()-unavailable fallback branch actually being entered) -- re-measured live at 372 pass / 0 fail / ~19.9-23.0s wall (three consecutive local runs) in the same commit that added them. Raised 372 -> 399 by 50-18's D-14 gap closure: 27 new cases -- bootstrap-redemption.test.mjs gained 21 (the already-bootstrapped networkHash assertion, the D-14 single-flight-replay seam test, and 19 source-level wiring tests + inertness controls for Bootstrap.tsx's three new optional props and main.tsx's onComplete/onAlreadyBootstrapped wiring and zero-timer state); refresh-swap.test.mjs gained 20 minus 14 net (two WR-11 behavioural close-forwarding tests, two per-hop source-pin tests plus an inertness control, and 12 DashboardShell.tsx source-wiring tests + inertness controls for the new pendingSwapContext classify effect) -- re-measured live at 399 pass / 0 fail / ~20.0s wall in the same commit range that added them. Raised 399 -> 439 by this gap-closure round's three parallel plans (50-20/50-22/50-23), consolidated into ONE re-measurement here per this round's own plan sequencing (50-24 was scoped to defer the baseline bump until after all three landed): 50-20 added the makeFakeTransport singleUse fixture tests plus the Bootstrap.tsx handedOffRef wiring tests (D-14/CR-02); 50-22 added the classify() catch/swap-error-banner tests, the handed-off-transport reset-coverage tests, and the withNetworkDbLifecycleLock lock-coverage tests over forgetNetwork/performOfficerSwap (CR-03/CR-04); 50-23 added the scopesResolved context/control wiring tests and the DashboardShell reset-block/provider-prop wiring tests (CR-01/D-18). 50-24 itself (this plan) added no new test:node cases -- its own new coverage is entirely in the browser gate (compose-gate.tsx's wire-call-count rungs and the new compose-preview-race phase, run-headless.mjs's --swap-only flag and PHASE 10) -- so this bump reflects only the prior three plans' work, re-measured live and unchanged by anything in this commit's own diff. Re-measured live at 439 pass / 0 fail / ~20.6s wall (`time yarn workspace votetorrent-dashboard test:node`, internal duration_ms ~19844-20077 across three consecutive local runs) in this commit. Gap-closure round 3's code-review fixes (CR-01 stale swapError, CR-02 double-confirm double-spend, WR-10 double-forget) added NO test:node cases and minPassing therefore stays at 439 -- re-measured live at 439 pass / 0 fail / ~20.4-20.5s wall across two runs in this commit range, NOT assumed unchanged. That is deliberate rather than an oversight: round 3's own WR-06 found that roughly 30 of the 40 tier-1 cases added the round before are `assert.match` regexes against a `.tsx` file read as a STRING, and neither Critical it found has a source-text signature -- state that is never written, and a handler that runs twice. Adding more matchers of that kind would have raised this number without raising coverage. The new evidence is entirely behavioural and lives in the browser gate: compose-gate.tsx's new compose-guards phase (PHASE 11, plus run-headless.mjs --guards-only, 14 rungs, two real networks) mounts the production DashboardShell and drives the actual state transitions, and each of its three discriminating rungs was measured in BOTH directions against a deliberately reverted fix. One of them was measured INERT on its first attempt and rewritten (waitForOrTimeout) rather than reported -- see the CR-02 commit message.",
    "measuredOn": "2026-09-02"
  },
  "uiWebTier1": {
    "command": "yarn workspace @votetorrent/ui-web test",
    "minPassing": 132,
    "maxFailing": 0,
    "maxSeconds": 15,
    "receiptMarker": "tier1-ui-web",
    "note": "53-13 adds the receiptMarker field (tier1-ui-web) and wires this entry into the renamed web-gates.yml's tier-1 job as its own keyed `assert-ci-baselines.mjs tier1 ... --baseline uiWebTier1` step for the first time -- CI enforcement of this entry begins here; the number itself is unchanged, re-confirmed live at 132 passing / 0 failing in the same session. Prior notes (129, 92, 42) follow verbatim, retained unedited.\n\nRe-measured 2026-09-02 at 132 passing / 0 failing / ~0.84s wall (`yarn workspace @votetorrent/ui-web test`), raising minPassing from 129. This is 53-11 (D-20): +1 in package-shape.test.mjs (rung 9, the new `./mutations` subpath's Node-importability and its four named exports); +3 in gate-source-integrity.test.mjs (the `styleSheets` check (1) rescoped to strip the new `--prove-token-missing` standing-witness exception before scanning, plus its own bounding test (1c) proving that exception contains exactly one occurrence, plus a sanity test (1d) proving the strip does not eat real code). dashboardTier1 stays UNCHANGED at 435 passing / 0 failing in the same session -- this plan adds no dashboard tier-1 assertion. Prior 129 note follows.\n\nRe-measured 2026-09-02 at 129 passing / 0 failing / ~0.79s wall (`yarn workspace @votetorrent/ui-web test`), raising minPassing from 99. This is 53-08 (D-19/D-23): +5 in the new test/serve-dist.test.mjs (the extname('/') trap, a .css content-type case, favicon 204, a missing file 404, and a real traversal 403 with its inside-the-root 200 negative control -- the traversal request percent-encodes its separators so it survives the HTTP client's own URL normalisation and is only reassembled by serve-dist.mjs's own decode step, after which the path.relative containment check catches it); +14 in the new test/tokens-parse.test.mjs (parseTokensCss/normaliseTokenValue/compareToken/hexToRgb/BASE_RULE_CHECKS, positive-control-first against the real tokens.css and small planted fixtures); +11 in the new test/gate-source-integrity.test.mjs (the three vacuous-shape guards -- a styleSheets count check, an interpolated rung name, a carried token-list literal -- plus the record()/RUNG_IDS bidirectional membership check and the compareToken body-shape check, each matcher proven against a planted violation first). dashboardTier1 rose in the SAME plan -- see that entry's own note. Prior 99 note follows.\n\nRe-measured 2026-09-02 at 99 passing / 0 failing / ~0.55-0.60s wall (`yarn workspace @votetorrent/ui-web test`), raising minPassing from 92. This is 53-07 (D-07/D-08/D-12/D-14/D-16/D-18): +7 in the new packages/ui-web/test/public-voice.test.mjs (the banned-lexeme and no-authority-derivation lints over the ten public.*/advisory.public.body keys this plan added, each positive-control-first, both demonstrated live to FAIL against the real advisory.authority.body value -- substituting it in place of advisory.public.body and separately planting the word 'officer' into public.election.unreadableAddress.body). copy.test.mjs's and shared-components.test.mjs's own hard-coded 73-key assertions were updated in place to 83 (COPY's new total) and the D-25/D-07 key-set-identity check was extended to allow the ten new keys as a named delta -- neither edit added a new test case, so this bump is exactly +7. votetorrent-public's own test:node count rose 27 -> 79 in the same plan (election-address.test.mjs +23, election-shell.test.mjs +17, election-harness.test.mjs +12) -- no ci-baselines.json row exists yet for that app's tier-1 suite (53-06 deferred creating one to 53-13, and this plan's own scope note in 53-07-PLAN.md Task 3 says wiring a new entry into assert-ci-baselines.mjs's tier1 evaluator is 53-13's job); the live 79/0 count is recorded here and in 53-07-SUMMARY.md for 53-13 to consume. dashboardTier1 was re-measured live in this same session and confirmed UNCHANGED at 426 passing / 0 failing -- this plan touches no dashboard source file. Prior 92 note follows.\n\nRe-measured 2026-09-02 at 92 passing / 0 failing / ~0.57s wall (`yarn workspace @votetorrent/ui-web test`), raising minPassing from 42. This is 53-05 (D-01/D-02/D-07/D-12/D-19/D-25): +32 in the new packages/ui-web/test/election-phase.test.mjs (the 32 election-phase.js unit cases moved verbatim from the dashboard, imports repointed to package-relative siblings, no Database/quereus/seed-fixture dependency added); +16 in the new packages/ui-web/test/shared-components.test.mjs (source and behaviour assertions for AdvisoryDisclosure/LifecyclePill/DetailsToggle and the D-07 copy-key resolution, ten assertions plus their positive/inertness controls); +2 in copy.test.mjs (the D-25/D-07 single-delta key-set-identity check against 8326185d, narrowing what 53-05's own plan text anticipated as an existing 53-04 blanket key-set-equality check -- measured live that no such check actually existed in 53-04's landed suite, so this plan authored it fresh rather than narrowing a prior one; both directions of its discrimination proof were run live: planting a throwaway advisory.zz.body key in packages/ui-web/src/copy.js dropped the suite to 90 pass / 2 fail naming the unexpected key, and removing the plant restored 92/0). package-shape.test.mjs's rung 5 was also updated in the same commit range (four exports keys, not three, once ./lifecycle was added) -- a direct, in-scope consequence of 53-05's own package.json change, not a new test case. Prior 42 note follows.\n\nNew entry, 53-04 (D-05/D-09/D-10/D-11/D-25). Measured live 2026-09-02 at 42 passing / 0 failing / ~0.15s wall (`yarn workspace @votetorrent/ui-web test`). Prior to this plan the workspace already carried 17 cases (53-02's package-shape.test.mjs, 8 rungs; 53-03's tokens.test.mjs, 9 cases). This plan's Task 3 adds 25 more in the new packages/ui-web/test/copy.test.mjs: 16 moved verbatim from apps/VoteTorrentDashboard/test/node/copy.test.mjs (named in dashboardTier1's note above) plus 9 new -- the bare-literal 73-key count assertion (naming the three panelFrame.*Pill keys as the reason 70 is wrong), a positive control proving every header-contract matcher fires on a planted fixture, an inertness control proving the two absence matchers ((e) no completeness claim, (f) no single-app opener) can actually fail against a fixture that plants the forbidden phrase, and six D-10 header-contract assertions (two-consumer C2 + the word 'variant', the literal 'no longer the sole authority', contract C3 verbatim byte-for-byte, contract C4 verbatim byte-for-byte, absence of 'the table is complete as of this plan', absence of 'The single copy table for VoteTorrent Authority Dashboard'). Both discrimination demonstrations were run live and reverted in the same session: deleting 'no longer the sole authority' from packages/ui-web/src/copy.js dropped the suite to 41 pass / 1 fail; re-inserting 'The single copy table for VoteTorrent Authority Dashboard' as the header's second line also dropped it to 41 pass / 1 fail; the file was restored byte-identically after each (confirmed via `git status --porcelain` showing no diff) and the suite re-measured back to 42/0 both times. maxSeconds of 15 gives generous headroom over the measured ~0.15s.",
    "measuredOn": "2026-09-02"
  },
  "publicTier1": {
    "command": "yarn workspace votetorrent-public test:node",
    "minPassing": 80,
    "maxFailing": 0,
    "maxSeconds": 15,
    "receiptMarker": "tier1-public",
    "note": "New entry, added by 53-13, closing the gap 53-06 and 53-07 both explicitly deferred here: until now no ci-baselines.json row existed for votetorrent-public's tier-1 suite, so nothing could catch a silent drop in it. Live-measured 2026-09-02 at 80 passing / 0 failing / ~0.65s internal, ~1.7s wall (`yarn workspace votetorrent-public test:node`) -- the number is copied from neither 53-07's nor 53-09's nor 53-11's SUMMARY notes (all of which independently recorded 80/0 as unchanged since 53-09's app-shape.test.mjs inertness-control addition); it was re-run fresh in this commit. maxSeconds of 15 gives generous headroom in the same spirit as uiWebTier1's, over a suite two orders of magnitude faster than the budget. CI enforcement of this entry begins with 53-13's renamed web-gates.yml tier-1 job, which invokes this exact command and evaluates it via `assert-ci-baselines.mjs tier1 ... --baseline publicTier1`.",
    "measuredOn": "2026-09-02"
  },
  "relaySmoke": {
    "command": "node scripts/assert-relay-smoke.mjs (wraps: cd packages/p2p-probe-host && node relay-smoke.mjs)",
    "measuredOn": "2026-09-01",
    "measuredWallSecondsMin": 1.8,
    "measuredWallSecondsMax": 2.2,
    "hardTimeoutMs": 60000,
    "note": "PUB-03 (Node half) — the phase 40 requirement that packages/p2p-probe-host/relay-smoke.mjs reaches a live /p2p-circuit reservation ('RELAY SMOKE: PASS') on published @serfab/cadre-core. This was 40-VALIDATION.md's only manual-only, zero-automated-sampling requirement; scripts/assert-relay-smoke.mjs closes that gap. Measured live 2026-09-01 on Node v22.15.0 against the CURRENT published substrate (@serfab 0.11.0, @optimystic 0.25.1, quereus 4.17.1 -- already moved well past the 0.8.1/0.14.1/4.3.1 versions the requirement text names): 6 consecutive runs (3 bare `node relay-smoke.mjs`, 3 through the wrapper) all PASS at 1.8-2.2s wall each, reservation observed via getMultiaddrs()/p2p-circuit at a consistent ~1.26-1.3s. relay-smoke.mjs's own internal RESERVATION_TIMEOUT_MS is 30000ms; hardTimeoutMs=60000 in the wrapper gives headroom over both figures for a slower CI runner while still bounding a genuine hang. This entry is a discipline record only -- scripts/assert-relay-smoke.mjs hardcodes HARD_TIMEOUT_MS inline (not read from this file) because assert-ci-baselines.mjs's five subcommands are all log-parsers over a captured run, and relay-smoke.mjs is instead spawned and asserted directly by its own wrapper; there is no 'relaySmoke' subcommand here. Discrimination proof (both directions, 2026-09-01): the wrapper was pointed at three fixture scripts standing in for relay-smoke.mjs (never relay-smoke.mjs itself, which was not modified) -- (a) prints 'RELAY SMOKE: FAIL' + exits 1 -> wrapper exit 1, correctly caught by the FAIL-marker branch; (b) exits 0 with NEITHER marker -> wrapper exit 1, correctly caught by the missing-marker branch (proves the exit code alone is not trusted); (c) prints nothing further and hangs, ignoring SIGTERM -> wrapper's SIGKILL fired at the (temporarily lowered, for test speed) timeout and exit 1. All three were RED as expected; the real relay-smoke.mjs run immediately after was re-confirmed GREEN, so the gate is not stuck red."
  },
  "authorityTypecheck": {
    "command": "yarn workspace votetorrent-authority typecheck",
    "measuredOn": "2026-08-25",
    "maxErrors": 51,
    "note": "This is a ceiling, not a floor: the producer half added to apps/VoteTorrentAuthority by this phase must introduce none. A drop below 51 means someone fixed something and the ceiling should be lowered in that same commit, not silently absorbed."
  },
  "notEnforcedHere": {
    "voterJest": {
      "failingSuites": 4,
      "cause": "uint8arrays",
      "measuredOn": "2026-08-25",
      "reason": "This phase (the authority web dashboard) touches nothing in apps/VoteTorrentVoter. Importing an unrelated red baseline into a gate whose whole value is trustworthiness would make the gate less useful, not more. Recorded here, and echoed to every CI run's job summary, precisely so it cannot be silently forgotten -- it is not this phase's to fix, and it is not this workflow's to run."
    }
  }
}
